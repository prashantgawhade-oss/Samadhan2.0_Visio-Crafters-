// src/components/ProfileCard.jsx
export default function ProfileCard({ name, bio, image }) {
  return (
    <div className="card">
      <img src={image} alt={name} width="120" style={{ borderRadius: "50%" }} />
      <h2>{name}</h2>
      <p>{bio}</p>
    </div>
  );
}